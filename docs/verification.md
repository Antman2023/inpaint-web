# 验证记录

## 2026-09-14

环境：Windows 上的 Chrome，Vite 开发服务器；桌面视口及 390 × 844 手机模拟视口。手机模拟仍使用桌面机器的计算资源，不代表真实手机的推理性能。

### 自动检查

- `npm run check`：代码检查、112 项回归测试、TypeScript 检查和生产构建通过。
- 下载响应新增覆盖：部分响应、HTML/JSON/XML 错误页、响应流取消和释放、备用地址恢复、两处地址失败后的重试、普通二进制响应兼容性。

### 浏览器检查

- 首页加载时 `ort` 尚未加载；选择 dog 示例后正常进入编辑器。
- 真实图片修复：在 dog 示例上通过指针事件触发一次涂抹，真实模型生成编辑步骤 1。此次指针事件由脚本触发，未覆盖物理触摸设备的指针捕获行为。
- 超分下载取消：下载中取消后保留当前修复结果和历史，工具栏恢复操作。
- 真实 4 倍超分：重新进入 dog 示例，处理 24 个分块后，图片从 275 × 183 变为 1100 × 732；使用真实模型，无推理替身。
- 撤销回到 275 × 183，重做恢复 1100 × 732，历史选中状态同步变化。
- 原图对比滑块按 End 后值为 100，原图裁剪为 `inset(0px 0px 0px 100%)`。
- 390 × 844 视口中，编辑工具栏边界为 x=12、y=642、宽=366、高=190，完整位于视口中；页面没有横向溢出。
- 700px 视口中，修复前示例列表第一张左边缘为 -78px、容器左边缘为 32px，且已无法向左滚动。修复后第一张左边缘为 36px；滚动到末端时，最后一张右边缘为 664px、容器右边缘为 668px，首尾图片均完整可达。
- 浅色主题 Lighthouse 手机快照的可访问性分数由 95 提升至 100；原有强调文字对比度约 1.1:1 的失败项消除。处理中的编辑器快照同样为 100。快照分数不等于完整人工可访问性审计。
- 真实运行环境修复：从已有超分结果进入“一键修复运行环境”，完成后图片 URL、历史结果和对比滑块位置保持不变，焦点返回“问题反馈”按钮。页面加载 `ort.wasm.min.js`，`ort.env.wasm.numThreads` 为 1；撤销到原图后再次涂抹，真实推理成功生成编辑步骤 2。
- 568 × 320 横屏首页：修复前标题 y=-13px 且容器已经滚到顶部，无法访问标题；修复后标题 y=116px，滚动到底部时示例区下边缘为 296px，位于 320px 视口内。
- 320 × 568 竖屏首页：顶部内容 y=88px；滚动到底部后示例区下边缘为 544px，页面无横向溢出。
- 1440 × 900 桌面首页：内容上下留白均为 109px，保持居中，没有额外的纵向或横向溢出。

### 尚未覆盖

- Firefox、Safari、真实触摸设备和低内存设备。
- 最大尺寸图片的处理性能和长时间连续编辑。

浏览器控制台另有缺失 favicon 的 404 和 ONNX 节点执行提供程序分配警告；本次修复和超分均成功完成。
